node_modules/
dist/
.next/

# skills-cli/skills is a build output — the canonical pack is /skills, mirrored
# in by skills-cli/scripts/sync-skills.mjs on build and again on prepack
/skills-cli/skills/
.pytest_cache/
__pycache__/
*.egg-info/
*.log

.hch

# generated by scripts/live-loop.sh
.env.smcraft-live

# session-local launchers and caches (host-specific, never publishable)
.worktrees/
.playwright-mcp/
.coaia/
START.sh
TUSHELL.sh
RESUME.sh
RESUME--*.sh

# the live demo document at repo root — mutable session state; durable machines live in episodes
/statemachine.smdf.json
LAUNCH-DESIGNER.sh
mcp-config-stateloom.json
