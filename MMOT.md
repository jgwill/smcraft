# MMOT

* Need to be capable to model internal state (parent state), somehow maybe that is a sub-diagram that opens and we can model the same type of state-machine internally, execting that the "End" of this inner-state is linked somehow to the exit or something like that

